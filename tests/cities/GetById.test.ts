import { StatusCodes } from "http-status-codes";
import { testServer } from "../jest.setup";

describe("Cities - get by id", () => {
  // TODO: should get a city by id
  // TODO: should try to get a city with an invalid id
});
