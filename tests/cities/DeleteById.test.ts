import { StatusCodes } from "http-status-codes";
import { testServer } from "../jest.setup";

describe("Cities - delete", () => {
  // TODO: should delete a city by id
  // TODO: should try to delete a city with an invalid id
});
